<h2>Users</h2>
<p class="text-muted">
  This page shows all user accounts and privileges. A "User" is someone that can log into Paddi, enter Matches, and perform other actions.
</p>
<br/>
<%= link_to 'New user', new_user_path, { :class=>"btn btn-primary" } if current_user.id==1 %>
<br/><br/>


<table class="table table-striped table-sm">
<thead>
  <tr>    
    <th>Name</th>
    <th>Username</th>
    <th>Last Login</th>
    <th>Role</th>      
    <th colspan="1"></th>
  </tr>
</thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <%= render user %>
      </tr>
    <% end %>
  </tbody>
</table>

<br/><br/><br/><br/><br/><br/>
<%= link_to 'Reset Players and Delete All Matches', reset_system_path, :data => { :confirm => "Are you sure?" }, :method => :delete, :class => 'btn-outline-danger button-xs' if current_user.id==1 unless Rails.env.production? %>
<br/><br/>
